"""User-defined strategies.

Drop any module containing a :class:`~interfaces.strategy.BaseStrategy`
subclass into this package.  ``main.py`` will discover and wire them up
automatically — no registration step required.
"""
